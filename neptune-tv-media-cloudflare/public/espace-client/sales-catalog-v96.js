const RELEASE='neptune-sales-tunnel-20260811-v96';
const API='/api/reservation/catalog-v96';
let busy=false,lastSig='';boot();
function boot(){document.readyState==='loading'?document.addEventListener('DOMContentLoaded',mount,{once:true}):mount();new MutationObserver(()=>requestAnimationFrame(mount)).observe(document.documentElement,{subtree:true,childList:true});}
async function mount(){const panel=document.querySelector('.formats-panel'),grid=panel?.querySelector('.format-grid');if(!panel||!grid||busy)return;busy=true;try{const r=await fetch(API,{credentials:'same-origin',cache:'no-store',headers:{Accept:'application/json'}}),d=await r.json().catch(()=>({}));if(!r.ok||!d.cities?.length)return;const sig=JSON.stringify(d.cities.map(c=>[c.id,c.formats?.map(f=>[f.id,f.offers?.length])])) ;if(sig===lastSig&&grid.dataset.salesCatalogV96)return;lastSig=sig;panel.dataset.salesCatalogV96='';document.body.dataset.salesCatalogRelease=RELEASE;const title=panel.querySelector('h2,h3');if(title)title.textContent='Réserver un autre passage';const intro=panel.querySelector('p');if(intro)intro.textContent='Choisissez d’abord la ville, puis le format disponible. Les offres et paiements sont automatiquement synchronisés avec Neptune Media.';grid.classList.add('v96-client-city-grid');grid.dataset.salesCatalogV96='';grid.innerHTML=d.cities.map(city=>`<section class="v96-client-city"><header><span>VILLE</span><h3>${esc(city.name)}</h3><p>${esc(city.country||'France')}</p></header><div class="v96-client-formats">${(city.formats||[]).map(f=>card(city,f)).join('')}</div></section>`).join('');document.querySelectorAll('.client-media-catalog-v95').forEach(x=>x.remove());}finally{busy=false;}}
function card(city,f){const prices=(f.offers||[]).map(o=>Number(o.clientPriceCents||0)).filter(Boolean),min=prices.length?Math.min(...prices):0;const url=new URL('/reserver',location.origin);url.searchParams.set('city',city.slug);url.searchParams.set('format',f.slug);return `<article class="v96-client-format"><div><small>${esc(f.concept||'NEPTUNE MEDIA')}</small><h4>${esc(f.name)}</h4>${f.durationLabel?`<span>${esc(f.durationLabel)}</span>`:''}<p>${esc(f.description||'')}</p></div><footer><strong>${min?`À partir de ${money(min)}`:'Voir les offres'}</strong><a href="${esc(url.toString())}">Choisir</a></footer></article>`;}
function money(c){return new Intl.NumberFormat('fr-FR',{style:'currency',currency:'EUR',maximumFractionDigits:0}).format(Number(c||0)/100);}function esc(v){return String(v??'').replace(/[&<>"']/gu,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot',"'":'&#39;'}[c]));}
