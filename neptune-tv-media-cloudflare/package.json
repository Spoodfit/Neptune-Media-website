{
  "name": "neptune-media-cloudflare",
  "private": true,
  "version": "4.3.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "npm run verify && wrangler deploy",
    "deploy:temporary": "npm run verify && wrangler deploy --temporary",
    "check": "find src public scripts -type f -name '*.js' -print0 | xargs -0 -n1 node --check && node scripts/verify-studio-unified.mjs && node scripts/verify-drive-sync.mjs && node scripts/verify-content-library.mjs",
    "test:smoke": "node scripts/smoke-test.mjs",
    "verify": "npm run check && npm run test:smoke && wrangler deploy --dry-run"
  },
  "devDependencies": {
    "wrangler": "^4.111.0"
  }
}
